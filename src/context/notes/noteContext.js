import { createContext } from "react";
const noteContext= createContext();
//In this file we are telling react that give me context i want to use and the react give the context after creating 
// and importing that context in NoteState.js to use that context that much only

export default noteContext;
//This folder is used to create context only not more than that 